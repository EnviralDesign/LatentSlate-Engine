# Family/workflow provenance: ComfyUI workflow templates v0.1.37 establish the
# first/last-frame endpoint semantics. Engine instead uses the pinned native-BF16
# Diffusers ConditionPipeline with its product 24fps and distilled-default contract.
[runnable_recipe]
key = "ltx-2-3.image-to-video.native-distilled-bf16"
name = "LTX 2.3 Distilled — Image(s) to Video"
description = "LTX 2.3 family/workflow-derived native BF16 image-conditioned video with a required first frame, optional final-frame anchor, synchronized audio, and Engine 24fps defaults."
family = "ltx23"
base_tool = "ltx23.image_to_video"
tags = ["builtin", "family-derived", "native-diffusers", "distilled", "image-to-video", "first-last-frame"]

[runnable_recipe.model]
resource = "model:ltx23:diffusers--ltx-2.3-distilled-diffusers"

[runnable_recipe.optimizations]
attention = "native"
offload = "sequential"
quantization = "bf16"
cache = "prompt"
