# Pinned Comfy topology is intentionally not substituted here: its T2V graph uses
# Dev FP8 plus a Distilled LoRA, whereas this is the separate native-Diffusers
# Distilled BF16 reference closure. The runtime fixes the official Diffusers
# Distilled schedule (8 steps, CFG 1) and synchronized audio path.
[runnable_recipe]
key = "ltx-2-3.text-to-video.native-distilled-bf16"
name = "LTX 2.3 Distilled BF16 Reference — Text to Video"
description = "Dense Distilled BF16 Diffusers reference closure for synchronized text-to-video audio/video. This approximately 95 GB reference path is deliberately distinct from the practical Dev FP8 main denoiser plus Distilled LoRA recipe."
family = "ltx23"
base_tool = "ltx23.text_to_video"
tags = ["builtin", "reference", "native-diffusers", "distilled", "text-to-video", "synchronized-audio", "exact-closure"]

[runnable_recipe.model]
resource = "model:ltx23:diffusers--ltx-2.3-distilled-diffusers"

[runnable_recipe.optimizations]
attention = "native"
offload = "sequential"
quantization = "bf16"
cache = "none"
