# Built-in deployment profiles

Small curated recipe selections may live here. Profiles select runnable recipe keys;
they never contain credentials. Generated deployment locks capture the exact Engine
version and deduplicated resource closure for a target.
