# Greenfield repository guidance

This is a documentation-only reset. Do not infer an implementation architecture,
recipe format, runtime model, or build system from the pre-reset repository.

Use the documentation in `docs/` as product constraints only. Design and add new
implementation deliberately, with its own tests, build tooling, and operational
documentation. The historical checkpoint tag is available for recovery or
comparison, but it is not current design authority.
